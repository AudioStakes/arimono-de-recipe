# AGENTS.md

## Project: ありもの de レシピ プロンプトメーカー

このプロジェクトは、冷蔵庫にある材料や希望条件から、AI にレシピ提案を依頼するための文章を作る静的Webアプリである。

## Core Concept

- このアプリはレシピ生成アプリではなく、AI に貼るためのプロンプト生成アプリであること。
- アプリ内でAI生成やレシピ生成を行わないこと。
- ユーザーが入力した材料・条件を、AIに依頼しやすい文章へ整えることを目的とすること。
- 生成物は「コピーしてAIに貼る文章」であることが、UI上でも伝わること。

## Technical Constraints

- TypeScript / CSS / HTML を Vite で静的サイトとしてビルドすること。
- Cloudflare Pages で `dist/` を配信できる構成を維持すること。
- API連携を行わないこと。
- サーバー処理を行わないこと。
- ログイン機能を持たないこと。
- 入力内容を外部送信しないこと。
- 外部送信や保存を前提とする設計にしないこと。
- 依存を増やす場合は、静的フロントエンドとしての単純さを損なわないこと。

## UX Principles

- スマホで使いやすいUIを優先すること。
- 入力項目が多くても、初期表示は軽く見えるようにすること。
- 基本項目と詳細項目を分け、詳細条件は折りたたむこと。
- ユーザーが「いま入力した条件」と「できあがったプロンプト」の関係を視覚的に理解できること。
- フォーム入力後、プロンプトへ進みやすい導線を用意すること。
- コピーして使うアプリであるため、コピー操作は目立たせ、成功状態も明確に伝えること。

## Visual Direction

- 見た目は、あたたかいミニマルな方向性を維持すること。
- 派手なグラデーション背景は使わないこと。
- クリーム系背景、やわらかい影、大きめの角丸、オレンジ系アクセントを基本とすること。
- 家庭料理・夕方・冷蔵庫の余り物という利用シーンに合う、落ち着いた見た目にすること。
- アイコンは単色の線画SVGを基本とし、色や装飾を増やしすぎないこと。
- アイコンやテキストの縦位置・中央寄せは揃えること。

## Input Design

- 材料を含む複数入力項目は、候補選択と自由入力の両方を許可すること。
- 複数入力項目は、入力値をピル化するUIを基本とすること。
- 候補入力にはネイティブdatalistではなく、独自の候補リストを使うこと。
- 候補リストは、入力欄クリック、右端の▽、キーボード操作で開けること。
- 候補選択後やEnter確定後は、入力中テキストを空にすること。
- 同じ値を重複して追加しないこと。
- ピル削除は、×クリックとキーボード操作の両方に対応すること。
- 空の入力欄でBackspace/Deleteを押した場合、まず直前のピルにフォーカスし、もう一度押すと削除する安全な挙動にすること。

## Prompt Generation Rules

- 未指定項目はプロンプトに出力しないこと。
- 未指定項目に対応するルール文も出力しないこと。
- プロンプトは短く、AIへの不要な指示を減らすこと。
- 指定された材料・条件は強く守らせること。
- 家にない材料を主材料として追加させないこと。
- 条件的に難しい場合は、無理に提案せず理由を短く説明させること。
- 足りない材料、代用案、保存方法は不要と明示すること。
- 一緒に出す料理が指定された場合は、その料理との相性を考慮した出力形式にすること。
- 一緒に出す料理が未指定の場合は、相性に関する出力項目を出さないこと。

## Testing / Maintainability

- 仕様変更時は、既存仕様を壊さないこと。
- 仕様が複雑になった箇所は、自動テストまたはセルフテストで確認できるようにすること。
- テストは、DOM構造、入力UI、状態表示、プロンプト生成、コピーUI、レスポンシブ、HTML健全性に分けて考えること。
- 既存テストは、明らかに誤っている場合を除き変更しないこと。
- 新しい仕様を追加したら、それに対応するテスト観点も追加すること。
- 不要になった旧UI、旧文言、未使用コードは残さないこと。
- できるだけDRYに保ち、フォーム項目・候補・プロンプト出力ルールは設定データ中心に管理すること。
- HTMLとJavaScriptが構文エラーなく実行される状態を常に保つこと。
