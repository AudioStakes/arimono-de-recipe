# AGENTS.md

## Project

このプロジェクトは、「ありもの de レシピ」です。

冷蔵庫にある食材・材料やレシピ要望から、AI に貼り付けるレシピ依頼文を作る静的 Web アプリです。

## Read first

- ドメイン用語は `CONTEXT.md` を参照する。
- 重要な設計判断の背景は `docs/adr/` を参照する。
- `AGENTS.md` は、AI エージェントや開発者が作業するときの最小限のルールを定義する。
- 用語の定義や設計判断の背景を `AGENTS.md` に重複して増やさない。

## Non-negotiable rules

- このアプリは、レシピ生成アプリではない。
- アプリ内で AI 生成やレシピ生成を行わない。
- API 連携を行わない。
- サーバー処理を行わない。
- ログイン機能を持たない。
- 入力内容を外部送信しない。
- 外部送信や保存を前提とする設計にしない。
- 未指定項目はプロンプトに出力しない。
- 未指定項目に対応するルール文も出力しない。
- 家にない材料を主材料として追加させるレシピ依頼文にしない。

## Commands

```bash
npm install
npm run dev
npm run format:check
npm run lint
npm run typecheck
npm run build
npm run test
npm run check
```

Use focused commands while developing:

```bash
npm run test:unit
npm run test:e2e
npm run test:e2e:ui
```

Before finishing a meaningful change, prefer running:

```bash
npm run check
```

If `npm run check` is too broad for the change, run the smallest relevant subset and report what was run.

## Implementation rules

- TypeScript / CSS / HTML を Vite で静的サイトとしてビルドする構成を維持する。
- Cloudflare Pages で `dist/` を配信できる構成を維持する。
- 依存を増やす場合は、静的フロントエンドとしての単純さを損なわない。
- できるだけ DRY に保ち、レシピ項目、候補値、プロンプト出力ルールは設定データ中心に管理する。
- 不要になった旧 UI、旧文言、未使用コードは残さない。
- HTML と JavaScript が構文エラーなく実行される状態を常に保つ。

## Recipe item changes

- レシピ項目の追加・削除・統合・名称変更では、`CONTEXT.md` の用語体系と一致させる。
- レシピ項目を増やす前に、既存項目へ統合できないか検討する。
- フィールド数はできるだけ少なく保つ。
- 基本項目は、ユーザーが最初に入力する可能性が高いものに絞る。
- こだわり項目は、必要なときだけ追加で指定するものにする。
- レシピ項目の変更時は、UI、候補値、指定値の表示、プロンプト出力、テスト観点をあわせて確認する。
- UI 表示名と内部実装名が異なる場合でも、ユーザーに見える文言は `CONTEXT.md` の用語体系を優先する。

## Input behavior

- 複数の値を扱うレシピ項目では、候補値の選択と自由入力値の入力を両方許可する。
- 候補値にない内容でも、ユーザーが自由入力値として指定できる状態を保つ。
- 入力中の値と指定値を混同しない。
- 候補絞り込みは、入力文字や読みから候補値を絞り込む。
- 同じ値を重複して追加しない。
- 候補値を選択した後や Enter で確定した後は、入力中の値を空にする。
- 候補リストは、入力欄クリック、候補表示ボタン、キーボード操作で開ける。
- 空の入力欄で Backspace / Delete を押した場合は、誤削除を避ける安全な挙動にする。
- 日本語入力中に、IME 確定前の Enter を指定値の追加として誤処理しない。
- 候補付き入力欄の入力挙動は `src/combo-registry.ts` を中心に管理し、`app-events.ts` に IME・候補開閉・指定値編集の分岐を戻さない。

## Prompt generation

- プロンプトは短くし、AI への不要な指示を増やさない。
- 指定されたレシピ要望は強く守らせる。
- 指定された食材・材料を中心に使わせる。
- 指定された料理区分・作りたいものだけを提案させる。
- 指定された調理方法・調理器具だけで作れる手順にする。
- 指定された調理時間がある場合は、現実的に収まる手順にする。
- NG食材・調味料に指定されたものは使わせない。
- 合わせたい料理・一緒に出す料理が指定された場合は、その料理との味・食感・量の相性を考慮させる。
- 合わせたい料理・一緒に出す料理が未指定の場合は、相性に関する出力項目を出さない。
- 条件的に難しい場合は、無理に提案せず、理由を短く説明させる。
- 足りない材料、代用案、保存方法は不要と明示する。

## UX and visual direction

- スマホで使いやすい UI を優先する。
- 入力項目が多くても、初期表示は軽く見えるようにする。
- 基本項目とこだわり項目を分け、こだわり項目は折りたたむ。
- ユーザーが「指定値」と「できあがったレシピ依頼文」の関係を理解できるようにする。
- コピーして使うアプリであるため、コピー操作は見つけやすくし、成功状態も明確に伝える。
- 見た目は、あたたかいミニマルな方向性を維持する。
- 派手なグラデーション背景は使わない。
- クリーム系背景、やわらかい影、大きめの角丸、オレンジ系アクセントを基本とする。
- アイコンは単色の線画 SVG を基本とし、色や装飾を増やしすぎない。

## Testing and maintainability

- 仕様変更時は、既存仕様を壊さない。
- 仕様が複雑になった箇所は、自動テストまたはセルフテストで確認できるようにする。
- 既存テストは、明らかに誤っている場合を除き変更しない。
- 新しい仕様を追加したら、それに対応するテスト観点も追加する。
- Playwright の一部テストが不安定で時間を使い過ぎる場合は、理由を `FIXME` コメントに残して一旦 `skip` し、後で再開しやすくする。
- 候補付き入力欄の仕様変更時は、まず小さな単位で入力挙動を確認し、そのうえで画面全体の E2E を確認する。

## Change checklist

レシピ項目や候補値を変更するときは、少なくとも以下を確認する。

- `CONTEXT.md` の用語と矛盾していないか。
- 画面文言がユーザーに分かりやすいか。
- 候補値の分類が自然か。
- 指定値の表示が分かりやすいか。
- プロンプト出力に反映されるか。
- 未指定項目が出力されないか。
- 関連するテスト観点を追加または更新したか。
