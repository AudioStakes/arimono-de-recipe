# Prioritize lightweight mobile-friendly UI

The app should prioritize a lightweight mobile-friendly interface because users are likely to build recipe requests quickly while looking at ingredients at home. We keep the initial form visually light by separating `基本項目` from `こだわり項目`, make the relationship between specified values and the generated recipe request clear, and use a warm minimal visual direction rather than a dense search-form interface.
